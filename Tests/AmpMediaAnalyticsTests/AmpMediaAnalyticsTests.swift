import XCTest
@testable import AmpMediaAnalytics

final class AmpMediaAnalyticsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AmpMediaAnalytics().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
